name: Conditional Workflow Execution

on:
  push:
    branches:
      - main
      - 'feature/*'

jobs:
  production-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Print message
        run: echo "Workflow triggered because push happened on main or feature branch"
